# @smallstack/eslint-config
Exports all smallstack eslint rules and eslint configuration.

## How-to install
`npm install  --save-dev @smallstack/eslint-config`

## How-to use
reference this package in .eslintrc.json like:

{
    "extends": ["@smallstack"]
}
